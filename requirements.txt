# NLP libraries
torch>=2.0.0
transformers>=4.30.0
huggingface_hub>=0.20.0
spacy>=3.5.0
# Fixes C-header binary incompatibility bug with SpaCy pipelines
numpy>=2.0.0
wordfreq>=3.0.0

# Semantic filtering (optional, ~7GB model per language)
# Uncomment to enable semantic_filter feature:
# fasttext>=0.9.2

# German Neural NLP (Stanford Stanza)
stanza>=1.8.0

# (Note: SpaCy is still used for English and requires english models loaded via code)
